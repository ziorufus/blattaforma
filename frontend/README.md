# Blattaforma — frontend

Vue 3 + Vite + Bootstrap 5. Vedi il [README principale](../README.md) per
setup, variabili d'ambiente e come aggiungere un modulo.

```bash
npm install
cp .env.example .env
npm run dev
```
